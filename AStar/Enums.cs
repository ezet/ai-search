namespace eZet.AStar {
    /// <summary>
    /// Node states, used for GUI binding
    /// </summary>
    public enum NodeState {
        Undiscovered,
        Open,
        Processing,
        Closed,
        Solution
    }
}